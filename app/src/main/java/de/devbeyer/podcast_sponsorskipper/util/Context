ackage de.devbeyer.podcast_sponsorskipper.util

import android.content.Context

fun Context.openLink(url: String) {
    val intent = Intent(Intent.ACTION_VIEW).apply {
        data = Uri.parse(url)
    }
    startActivity(intent)
}